# Delta Exchange API — required for live market data and trading
# Sign up at https://www.delta.exchange and generate API keys
# Do NOT commit real keys. Copy this file to .env and fill in values.
DELTA_API_KEY=your_delta_api_key
DELTA_API_SECRET=your_delta_api_secret
DELTA_API_BASE_URL=https://api.delta.exchange

# CoinGecko (free tier, no key required)
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3

# JWT Authentication Secret (any random string)
JWT_SECRET=your_jwt_secret_here